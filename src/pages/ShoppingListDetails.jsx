import React from "react";
import ShoppingDetails from "../features/ShoppingLists/ShoppingDetails";

const ShoppingListDetails = ({ activeUser }) => {
  return <ShoppingDetails activeUser={activeUser} />;
};

export default ShoppingListDetails;
